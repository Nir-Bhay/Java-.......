import java.util.Arrays;

public class arrayshorting {
    public static void main(String[] args) {

        // reverse the string
        String intron = " helor fir";
        String[] intro = intron.split(" ");


        System.out.println(intron.charAt(5));

        // int[] arr = { 2, 3, 4, 23, 12, 4, 2 };
        // // Arrays.sort(arr);
        // // System.out.println(Arrays.toString(arr));

        // int temp = 0;
        // for (int i = 0; i <= arr.length - 1; i++) {
        // for (int j = i + 1; j <= arr.length - 1; j++) {
        // if (arr[i] > arr[j]) {
        // temp = arr[i];
        // arr[i] = arr[j];
        // arr[j] = temp;
        // }
        // }
        // }

        // System.out.println(Arrays.toString(arr));
    }
}
